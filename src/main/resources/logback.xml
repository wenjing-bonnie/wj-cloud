<?xml version="1.0" encoding="UTF-8"?>
<!--scan:配置文件如果发生改变，将会被重新加载；scanPeriod:配置文件是否有修改的时间间隔-->
<configuration scan="true" scanPeriod="60 seconds">
    <!--日志输出路径-->
    <property name="LOG_PATH" value="../../logs/wj-cloud"/>
    <!--输出到控制台-->
    <appender name="stdout" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>info</level>
        </filter>
        <encoder>
            <pattern>>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger - %msg%n</pattern>
        </encoder>
    </appender>
    <!--核心系统日志输出到文件、基于日志的大小和时间归档-->
    <appender name="rollingFilter" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- ${LOG_PATH}/hrt-platform.%d{yyyy-MM-dd.HH}.log -->
            <fileNamePattern>${LOG_PATH}/j1-cloud-eureka-server.%d{yyyy-MM-dd.HH}.%i.log
            </fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <!--控制单个文件的最大值-->
                <maxFileSize>50M</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <!--最多保留30天日志文件-->
            <maxHistory>100</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger - %msg%n</pattern>
        </encoder>
    </appender>
    <!--异步输出基础数据日志-->
    <appender name="async" class="ch.qos.logback.classic.AsyncAppender">
        <!-- 设置为0表示不丢失日志.默认值为20,当队列还剩20%容量时,则会丢弃TRACE、DEBUG、INFO级别的日志 -->
        <discardingThreshold>0</discardingThreshold>
        <!-- 更改默认的队列的深度,该值会影响性能.默认值为256 -->
        <queueSize>92160</queueSize>
        <!-- 添加到appender,最多只能添加一个 -->
        <appender-ref ref="rollingFile"/>
    </appender>
    <logger name="java.sql" level="DEBUG"/>
    <logger name="java.sql.ResultSet" level="DEBUG"/>
    <!--全局日志输出级别 -->
    <root level="info">
        <appender-ref ref="stdout"/>
        <appender-ref ref="rollingFile"/>
    </root>
</configuration>